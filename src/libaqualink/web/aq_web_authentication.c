#include "aq_web_authentication.h"

#include "aqualink.h"

static char* string_to_base64(const char* expected)
{
	UNREFERENCED_PARAMETER(expected);

	///TODO
	return 0;
}

int ask_for_authentication(void)
{
	int ret = 0;

	return ret;
}

int is_authenticated(void)
{
	int authenticated = 0;

	return authenticated;
}

char* load_pem_file(const char* pem_file)
{
	UNREFERENCED_PARAMETER(pem_file);

	string_to_base64(0);

	///TODO
	return 0;
}
